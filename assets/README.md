# assets/ — slide e deck

| Cartella | Cosa | Stato |
|---|---|---|
| `deck_v7/` | **Deck corrente** (25 slide B/N, complete-SOTA rework) — `deck_v7.pptx`/`.pdf` + `build_deck.py` + discorso | attivo |
| `deck_v6/` | Deck precedente (rework post-review Thomas) | superato da v7 |
| `slide_v5/` | Figure e build della v5 (maggio) | archivio |
| `_legacy_builds/` | Build one-off pre-v5 (pptxgenjs `build.js` + `package.json`, `_build_*.py`, `rezip_pptx.py`, `sota_highres_material_deck.pptx`) | archivio, non manutenuto |

Convenzioni deck (B/N minimal, una idea per slide, refs su ogni slide): vedi doc call in `../docs/01_calls/`.
