# eagle/ — script esperimenti sul server

Copia di consultazione degli script che girano su eagle in `~/experiments/` (copia canonica = server).
Risultati e conclusioni: `docs/04_planning/EXPERIMENTS_LOG.md`.

- `sanity_binary_pneo.py` — EXP-001, sanity run binario PNEO (ResNet-50 RGB)
- `baseline_swin_rsp_pneo.py` — baseline Swin-T+RSP, protocollo Gibellini two-step
